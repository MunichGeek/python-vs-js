let player_ages = [17, 24, 32, 50];
for (var age of player_ages) {
    if (age > 18 && age < 40)
        console.log(`Player with age ${age} can play football!`);
        console.log(`Age ${age} is great`);
}
